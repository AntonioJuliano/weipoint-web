deployment:
  production:
    branch: master
    commands:
      - npm run build && aws s3 sync build s3://weipoint/
  staging:
    branch: staging
    commands:
      - npm run build && aws s3 sync build s3://weipointstaging/
